stages:
  - build
  - test
  - security
  - deploy
  - inference

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH
      when: always

variables:
  IMAGE_TAG: "$CI_COMMIT_SHA"

default:
  before_script:
    - echo "branch=$CI_COMMIT_BRANCH sha=$CI_COMMIT_SHA"

########################
# BUILD (docker build & push)
########################
.build-template:
  stage: build
  before_script:
    - echo "branch=$CI_COMMIT_BRANCH sha=$CI_COMMIT_SHA"
    - docker info
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

build-backend:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/backend:$CI_COMMIT_SHA" -f backend/Dockerfile backend
    - docker push "$DOCKERHUB_USERNAME/backend:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - backend/**
        - .gitlab-ci.yml

build-web:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/web:$CI_COMMIT_SHA" -f infra/nginx/Dockerfile .
    - docker push "$DOCKERHUB_USERNAME/web:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - frontend/**
        - infra/nginx/**
        - .gitlab-ci.yml

build-execution:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/execution:$CI_COMMIT_SHA" -f execution/Dockerfile execution
    - docker push "$DOCKERHUB_USERNAME/execution:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - execution/**
        - .gitlab-ci.yml

build-feedback:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/feedback:$CI_COMMIT_SHA" -f feedback/Dockerfile feedback
    - docker push "$DOCKERHUB_USERNAME/feedback:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - feedback/**
        - .gitlab-ci.yml

########################
# TEST
########################
test-fast:
  tags: [ec2-mono]
  stage: test
  script:
    - echo "Fast tests for feature/infra/fix"
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^(feature|infra|fix)\/.+/'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml

test-full:
  tags: [ec2-mono]
  stage: test
  script:
    - echo "Full tests for develop/main/hotfix"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml

########################
# SECURITY SCAN
########################
cache:
  key: trivy-cache
  paths:
    - .trivycache/

trivy_scan_images:
  stage: security
  tags: [ec2-mono]
  needs:
    - job: build-backend
      optional: true
    - job: build-web
      optional: true
    - job: build-execution
      optional: true
    - job: build-feedback
      optional: true
  variables:
    TRIVY_SEVERITY: "CRITICAL,HIGH"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml
      variables:
        TRIVY_EXIT_CODE: "0"   # develop은 발견돼도 통과
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml
      variables:
        TRIVY_EXIT_CODE: "0"   # 운영은 차단 <- 임시 패스
  script:
    - trivy image --scanners vuln --severity $TRIVY_SEVERITY --exit-code $TRIVY_EXIT_CODE ${DOCKERHUB_USERNAME}/backend:${CI_COMMIT_SHA} || true
    - trivy image --scanners vuln --severity $TRIVY_SEVERITY --exit-code $TRIVY_EXIT_CODE ${DOCKERHUB_USERNAME}/web:${CI_COMMIT_SHA} || true
    - trivy image --scanners vuln --severity $TRIVY_SEVERITY --exit-code $TRIVY_EXIT_CODE ${DOCKERHUB_USERNAME}/execution:${CI_COMMIT_SHA} || true
    - trivy image --scanners vuln --severity $TRIVY_SEVERITY --exit-code $TRIVY_EXIT_CODE ${DOCKERHUB_USERNAME}/feedback:${CI_COMMIT_SHA} || true

# 파일/설정 스캔은 빌드와 무관하므로 needs 제거 (needs로 인해 "job does not exist" 터지는 것 방지)
trivy_fs:
  stage: security
  tags: [ec2-mono]
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml
  script:
    - trivy fs --severity HIGH,CRITICAL .

trivy_config:
  stage: security
  tags: [ec2-mono]
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml
  script:
    - trivy config .

########################
# DEPLOY (EC2에서 docker pull & compose up)
########################
.deploy-template:
  stage: deploy
  before_script:
    - mkdir -p ~/.ssh
    - ssh-keyscan "$HOST" >> ~/.ssh/known_hosts

deploy-foundation:
  needs:
    - job: build-execution
      optional: true
    - job: build-feedback
      optional: true
  extends: .deploy-template
  script:
    - echo "CI_PIPELINE_SOURCE=$CI_PIPELINE_SOURCE branch=$CI_COMMIT_BRANCH protected=$CI_COMMIT_REF_PROTECTED"
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_foundation.sh"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      when: manual
      allow_failure: true

deploy-dev:
  needs:
    - job: build-backend
      optional: true
    - job: build-web
      optional: true
    - job: build-execution
      optional: true
    - job: build-feedback
      optional: true
  extends: .deploy-template
  script:
    - echo "CI_PIPELINE_SOURCE=$CI_PIPELINE_SOURCE branch=$CI_COMMIT_BRANCH protected=$CI_COMMIT_REF_PROTECTED"
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_server.sh dev"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      changes:
        - backend/**
        - frontend/**
        - infra/**
        - execution/**
        - feedback/**
        - .gitlab-ci.yml

deploy-prod:
  needs:
    - job: build-backend
      optional: true
    - job: build-web
      optional: true
    - job: build-execution
      optional: true
    - job: build-feedback
      optional: true
  extends: .deploy-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_server.sh prod"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      when: manual
      allow_failure: true

########################
# ROLLBACK (manual)
########################
.rollback-template:
  stage: deploy
  before_script:
    - mkdir -p ~/.ssh
    - ssh-keyscan "$HOST" >> ~/.ssh/known_hosts

rollback-dev:
  extends: .rollback-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' bash /srv/app/scripts/rollback.sh dev"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'
      when: manual
      allow_failure: true

rollback-prod:
  extends: .rollback-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' bash /srv/app/scripts/rollback.sh prod"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      when: manual
      allow_failure: true

rollback-foundation:
  extends: .rollback-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' bash /srv/app/scripts/rollback.sh foundation"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      when: manual
      allow_failure: true

########################
# INFERENCE STAGE
########################
inference-deploy:
  stage: inference
  tags: [elice-container]
  variables:
    GIT_STRATEGY: "none"
    INF_DIR: "inference"
    DEPLOY_DIR: "/srv/app/app/inference"
    MODEL_DIR: "/home/elicer/models"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH =~ /^hotfix\/.+/'
      changes:
        - inference/**
  script:
    - set -euo pipefail

    - echo "Deploying inference service..."
    - echo "branch=$CI_COMMIT_BRANCH sha=$CI_COMMIT_SHA"

    # 1) Clean working dir
    - rm -rf repo
    - mkdir -p repo
    - cd repo

    # 2) Sparse checkout only inference/
    - git init
    - git remote add origin "$CI_REPOSITORY_URL"
    - git fetch --depth 1 origin "$CI_COMMIT_SHA"
    - git sparse-checkout init --cone
    - git sparse-checkout set "$INF_DIR"
    - git checkout -f FETCH_HEAD

    # 3) Stage copy to /tmp first (avoid "file has vanished" due to cleanup)
    - STAGE_DIR="/tmp/inference_stage_${CI_PIPELINE_ID}_${CI_JOB_ID}"
    - rm -rf "$STAGE_DIR"
    - mkdir -p "$STAGE_DIR"
    - cp -a "$INF_DIR/." "$STAGE_DIR/"

    # 4) Deploy with rsync
    - mkdir -p "$DEPLOY_DIR"
    - rsync -a --delete --ignore-missing-args --exclude='__pycache__/' --exclude='*.pyc' "$STAGE_DIR/" "$DEPLOY_DIR/" || { rc=$?; [ "$rc" -eq 24 ] && echo "rsync warning(code 24) ignored" && exit 0; exit "$rc"; }

    # 5) Python env + deps
    - cd "$DEPLOY_DIR"
    - export PYENV_ROOT="$HOME/.pyenv"
    - export PATH="$PYENV_ROOT/bin:$PATH"
    - eval "$(pyenv init -)"
    - pyenv activate venv
    - python -V
    - pip install --upgrade pip
    - pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128

    - echo "Unzipping models..."
    - cd "$MODEL_DIR"
    - unzip -q model_label.zip -d "$DEPLOY_DIR/models/label_model"
    - unzip -q text_label.zip -d "$DEPLOY_DIR/models/text_model"