stages:
  - build
  - test
  - deploy
  - inference

workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: always
    - if: '$CI_COMMIT_BRANCH'
      when: always
    - when: never

variables:
  IMAGE_TAG: "$CI_COMMIT_SHA"

default:
  before_script:
    - echo "source=$CI_PIPELINE_SOURCE branch=$CI_COMMIT_BRANCH sha=$CI_COMMIT_SHA"

# =====================
# BUILD (Docker build & push)
# =====================
.build-template:
  stage: build
  before_script:
    - docker info
    - test -n "$DOCKERHUB_USERNAME" || { echo "DOCKERHUB_USERNAME missing"; exit 1; }
    - test -n "$DOCKERHUB_TOKEN" || { echo "DOCKERHUB_TOKEN missing"; exit 1; }
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

# 배포 브랜치(develop/main)에서는 backend/web는 항상 빌드해서 SHA 태그가 반드시 존재하게 함
build-backend:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/backend:$CI_COMMIT_SHA" -f backend/Dockerfile backend
    - docker push "$DOCKERHUB_USERNAME/backend:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main"'
      when: on_success

build-web:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/web:$CI_COMMIT_SHA" -f infra/nginx/Dockerfile .
    - docker push "$DOCKERHUB_USERNAME/web:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main"'
      when: on_success

# foundation 쪽(execution/feedback)은 변경 있을 때만 빌드해도 되는데,
# foundation 배포가 돌 때는 반드시 존재해야 하므로, 아래 deploy-foundation-*의 rules와 changes를 "같게" 맞춤.
build-execution:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/execution:$CI_COMMIT_SHA" -f execution/Dockerfile execution
    - docker push "$DOCKERHUB_USERNAME/execution:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main"'
      when: on_success

build-feedback:
  extends: .build-template
  script:
    - docker build -t "$DOCKERHUB_USERNAME/feedback:$CI_COMMIT_SHA" -f feedback/Dockerfile feedback
    - docker push "$DOCKERHUB_USERNAME/feedback:$CI_COMMIT_SHA"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main"'
      when: on_success


# =====================
# TEST
# =====================
test:
  stage: test
  tags: [ec2-mono]
  script:
    - echo "Running tests"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH =~ /^(feature|infra|fix)\//'

# =====================
# DEPLOY (SSH → server scripts)
# =====================
.deploy-template:
  stage: deploy
  before_script:
    - mkdir -p ~/.ssh
    - ssh-keyscan "$HOST" >> ~/.ssh/known_hosts

# ✅ Foundation deploy (db + execution + feedback)
deploy-foundation-dev:
  extends: .deploy-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_foundation.sh"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop"'
  needs:
    - job: build-execution
    - job: build-feedback


deploy-foundation-prod:
  extends: .deploy-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_foundation.sh"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
  needs:
    - job: build-execution
    - job: build-feedback


# ✅ Server deploy (backend + web)
deploy-dev:
  extends: .deploy-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" \
        "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_server.sh dev"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop"'
  needs:
    - job: build-backend
    - job: build-web

deploy-prod:
  extends: .deploy-template
  script:
    - chmod 600 "$SSH_KEY"
    - ssh -i "$SSH_KEY" "$USER@$HOST" \
        "DOCKERHUB_USERNAME='$DOCKERHUB_USERNAME' DOCKERHUB_TOKEN='$DOCKERHUB_TOKEN' IMAGE_TAG='$CI_COMMIT_SHA' bash /srv/app/scripts/deploy_server.sh prod"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
  needs:
    - job: build-backend
    - job: build-web

# =====================
# INFERENCE (Elice sparse checkout)
# =====================
inference-deploy:
  stage: inference
  tags: [elice-container]
  variables:
    GIT_STRATEGY: "none"
    INF_DIR: "inference"
    DEPLOY_DIR: "/srv/app/app/inference"
    MODEL_DIR: "/home/elicer/models"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "main"'
      changes:
        - inference/**
        - .gitlab-ci.yml
  script: |-
    set -euo pipefail
    echo "Deploying inference service"

    rm -rf repo
    mkdir repo
    cd repo

    git init
    git remote add origin "$CI_REPOSITORY_URL"
    git fetch --depth 1 origin "$CI_COMMIT_SHA"
    git sparse-checkout init --cone
    git sparse-checkout set "$INF_DIR"
    git checkout -f FETCH_HEAD

    mkdir -p "$DEPLOY_DIR"
    rsync -a "$INF_DIR/" "$DEPLOY_DIR/"

    cd "$DEPLOY_DIR"
    export PYENV_ROOT="$HOME/.pyenv"
    export PATH="$PYENV_ROOT/bin:$PATH"
    eval "$(pyenv init -)"
    pyenv activate venv
    pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128

    unzip -oq "$MODEL_DIR/label_model.zip" -d "$DEPLOY_DIR/models/label_model"
    unzip -oq "$MODEL_DIR/text_model.zip"  -d "$DEPLOY_DIR/models/text_model"
