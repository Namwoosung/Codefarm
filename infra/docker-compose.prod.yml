services:
  backend:
    image: ${DOCKERHUB_USERNAME}/backend:${IMAGE_TAG}
    container_name: app-prod-backend
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    ports:
      - "8080:8080"
    networks:
      - appnet

  frontend:
    image: ${DOCKERHUB_USERNAME}/frontend:${IMAGE_TAG}
    container_name: app-prod-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
